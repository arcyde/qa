How to execute the codes

Install all the libs from requirements.txt -> pip install -r requirements.txt

Executing api_test/simple_dog_api_test

Open the "api_test" folder, run terminal/prompt and execute "pytest -v"

Executing bdd/simple_dog_api_test

Open the "bdd" folder, run terminal/prompt and execute "behave"